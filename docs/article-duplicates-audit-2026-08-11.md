# Аудит дублей артикулов — 11 августа 2026

Проверена production-база `/opt/clock-erp/instance/catalog.db` в режиме только чтения.
Сравнение выполнено среди всех товаров (включая архивные) после удаления
пробелов по краям артикула; регистр, внутренние пробелы, дефисы, точки и другие
символы не нормализовались. Все найденные карточки активны.

| Артикул | Количество | ID товаров |
| --- | ---: | --- |
| `011211757Lady222` | 3 | `9037`, `10887`, `10888` |

Существующие записи не изменялись, не объединялись и не перепривязывались.
Из-за legacy-дубля UNIQUE constraint в рамках задачи не создаётся; новые дубли
блокируются общей серверной логикой создания и редактирования товара.
