# Импорт каталога Bitrix в Vechasu

Дата выполнения: 20 июля 2026 года.

## Последовательность

Перед записью выполнен реальный `preview` 20 товаров. Он показал 20 новых
карточек, 0 конфликтов и 0 записей; `instance/catalog.db` до и после preview
отсутствовал.

Импорт выполнен последовательно:

| Запуск | Выбрано | Создано | Обновлено | Без изменений | Конфликты |
| --- | ---: | ---: | ---: | ---: | ---: |
| 5 активных | 5 | 5 | 0 | 0 | 0 |
| 20 активных | 20 | 15 | 0 | 5 | 0 |
| 100 активных | 100 | 80 | 0 | 20 | 0 |
| Категория `57` «Braun часы» | 34 | 22 | 0 | 12 | 0 |
| Все активные, итог после проверки идентичности | 1550 | 1428 | 0 | 122 | 0 |
| Неактивные, отдельный запуск | 3134 | 3134 | 0 | 0 | 0 |

При первой полной активной проверке обнаружено 16 ошибочных резервных
сопоставлений карточек с разными Bitrix ID. Импорт неактивных был остановлен.
Правило исправлено: непустые разные ID одного источника не объединяются по
XML_ID или названию без ручного подтверждения. Повторный активный запуск создал
16 недостающих карточек. Итоговое количество стало равно источнику — 1550.

## Итоговая SQLite-база

* товаров: 4684;
* активных: 1550;
* неактивных: 3134;
* категорий с товарами: 96 из 97 разделов Bitrix;
* типов свойств: 56;
* значений свойств: 262304 (56 на каждую карточку);
* изображений: 28893;
* цен продажи: 4684;
* торговых предложений: 0;
* дублей `(external_source, external_product_id)`: 0;
* конфликтов: 0;
* неуспешных запусков: 0;
* закупочных цен: 0.

У каждой карточки есть категория, изображение и цена. `PRAGMA quick_check`
вернул `ok`, `PRAGMA foreign_key_check` не нашёл нарушений.

## Резервные копии

Перед полной активной загрузкой:

`/var/backups/clock-erp-catalog/20260720-153127/catalog.db`

Перед отдельной загрузкой неактивных:

`/var/backups/clock-erp-catalog/20260720-153551-before-inactive/catalog.db`

Обе копии после создания совпали с исходной SQLite-базой по SHA-256.

## Контроль побочных эффектов

Хэши остальных файлов `instance` до и после импорта совпали. Read-only запрос
к МойСклад показывал `meta.size = 0` после контрольных этапов и после полной
загрузки. Код импортёра не вызывает клиент МойСклад и не содержит таблиц или
команд складских операций. Остатки, приходы, списания и данные МойСклад не
изменялись.

Production-сервис `clock-erp` остался активным. `/catalog` и тестовая карточка
товара возвращали HTTP 200; время серверного ответа в контрольном запросе было
0,032–0,039 секунды.
